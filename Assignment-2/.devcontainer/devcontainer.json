{
  "name": "ROS2 + PyTorch + pgvector",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "ros",
  "workspaceFolder": "/workspaces/DS685work/Assignment-2",
  "shutdownAction": "stopCompose",
  "postCreateCommand": "bash -lc 'source /opt/ros/humble/setup.bash && cd ros_ws && colcon build'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-azuretools.vscode-docker",
        "ms-vscode-remote.remote-containers",
        "ms-vscode.cpptools"
      ]
    }
  }
}
